services:
  - type: web
    name: sushi-website
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:static
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: / 