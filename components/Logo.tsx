import { Sparkles } from 'lucide-react';

export function Logo() {
	return (
		<div className="inline-flex items-center justify-center h-8 w-8 rounded-xl bg-white/10 text-white">
			<Sparkles size={18} />
		</div>
	);
}